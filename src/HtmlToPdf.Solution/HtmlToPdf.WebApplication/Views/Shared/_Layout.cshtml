<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>Planner Template</title>

    <style>

        body {
            font-family: Arial, Helvetica, sans-serif;
            font-size: 3em;
            margin-top: 0;
            margin-bottom: 0;
            padding-top: 0;
            padding-bottom: 0;
        }

        .new-page {
            break-after: right;
            clear: both;
        }

        h1, h2 {
            margin: 0;
            text-align: center;
        }

        h2 {
            font-size: 1.0em;
        }

        .day {
            border: 1px solid #555;
            float: left;
            font-size: 2em;
            font-weight: bold;
            margin: 10px;
            text-align: center;
            width: 200px;
            height: 200px;
            line-height: 200px;
        }

        .day-of-week {
            float: left;
            font-size: 1.3em;
            font-weight: bold;
            margin: 10px;
            text-align: center;
            width: 200px;
            height: 200px;
            line-height: 200px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

            table.rows {
                border: 1px solid #555;
                border-bottom: none;
            }

                table.rows, table.rows td {
                    border-collapse: collapse;
                }

                    table.rows td {
                        line-height: 2em;
                    }

                        table.rows td:nth-child(2),
                        table.rows th:nth-child(2) {
                            border-left: 1px solid #555;
                        }

        #to-do-list-links {
            margin-top: 200px;
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            text-align: center;
        }

            #to-do-list-links div {
                border: 1px solid #555;
                text-align: center;
                font-size: 1.5em;
                height: 200px;
                line-height: 200px;
                margin: auto;
                width: 700px;
            }


        #month-links {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            column-gap: 20px;
            margin-top: 100px;
            row-gap: 50px;
            font-size: 2em;
            font-weight: bold;
            text-align: center;
        }

            #month-links a {
                border: 1px solid #555;
                height: 300px;
                line-height: 300px;
                margin: auto;
                text-align: center;
                width: 300px;
            }

        a.month-link {
            float: right;
            position: relative;
            top: -65px
        }

        th.heading {
            border-bottom: 1px solid #555;
            padding-left: 20px;
            text-align: left;
        }

        .bottom-line {
            border-bottom: 1px solid #555;
        }

        .top-left {
            float: left;
            position: relative;
            top: -60px
        }

    </style>
</head>
<body>

    <div>
        <main role="main">
            @RenderBody()
        </main>
    </div>

</body>
</html>
